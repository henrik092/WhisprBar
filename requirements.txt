numpy
sounddevice
pystray
Pillow
pyperclip
openai
elevenlabs
webrtcvad
pynput
noisereduce

# Optional local/offline transcription backends are intentionally not part of
# the default install because they pull large native dependencies and can drift
# independently from the core app:
#   pip install ".[faster-whisper]"
#   pip install ".[sherpa]"

# Testing dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
